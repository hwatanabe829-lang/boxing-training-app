<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<meta name="theme-color" content="#0d0f14">
<title>BOXER HIIT 20/10 — 立ったままタバタ</title>
<style>
:root{
  --bg:#0d0f14; --bg2:#151922; --card:#1b202b; --line:#2a3140;
  --txt:#eef2f8; --sub:#93a0b5;
  --red:#ff3b3b; --orange:#ff8a3d; --green:#22d39a; --blue:#4aa8ff; --yellow:#ffd23d;
  --safe-b: env(safe-area-inset-bottom, 0px);
}
*{box-sizing:border-box;-webkit-tap-highlight-color:transparent}
html,body{margin:0;padding:0;height:100%}
body{
  background:var(--bg); color:var(--txt);
  font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","Hiragino Kaku Gothic ProN","Yu Gothic",Meiryo,sans-serif;
  overscroll-behavior:none; user-select:none;
}
.wrap{max-width:520px;margin:0 auto;padding:0 16px calc(24px + var(--safe-b));}
.screen{display:none}
.screen.on{display:block}

/* ---------- ヘッダー ---------- */
header{
  position:sticky;top:0;z-index:20;background:linear-gradient(180deg,#0d0f14 70%,rgba(13,15,20,0));
  padding:18px 0 10px;
}
.logo{display:flex;align-items:center;gap:10px}
.logo .g{
  width:38px;height:38px;border-radius:11px;display:grid;place-items:center;font-size:20px;
  background:linear-gradient(135deg,var(--red),var(--orange));box-shadow:0 6px 18px rgba(255,59,59,.35)
}
.logo h1{font-size:19px;margin:0;letter-spacing:.04em}
.logo p{margin:2px 0 0;font-size:11px;color:var(--sub);letter-spacing:.08em}

/* ---------- 今日の記録 ---------- */
.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:14px 0 18px}
.stat{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:10px 8px;text-align:center}
.stat b{display:block;font-size:20px;line-height:1.1}
.stat span{font-size:10px;color:var(--sub)}

/* ---------- 見出し ---------- */
.h{display:flex;align-items:center;justify-content:space-between;margin:22px 0 10px}
.h h2{font-size:14px;margin:0;letter-spacing:.1em;color:var(--sub)}
.h button{background:none;border:none;color:var(--blue);font-size:12px;cursor:pointer;padding:4px}

/* ---------- プログラムカード ---------- */
.cards{display:flex;flex-direction:column;gap:10px}
.card{
  display:flex;align-items:center;gap:13px;width:100%;text-align:left;cursor:pointer;
  background:var(--card);border:1px solid var(--line);border-radius:16px;padding:14px;color:var(--txt);
  transition:transform .1s, border-color .15s;
}
.card:active{transform:scale(.98)}
.card .ico{
  width:46px;height:46px;flex:0 0 46px;border-radius:13px;display:grid;place-items:center;font-size:22px;
  background:#232a38
}
.card .t{flex:1;min-width:0}
.card .t b{display:block;font-size:15px;margin-bottom:3px}
.card .t small{display:block;color:var(--sub);font-size:11.5px;line-height:1.45}
.card .m{font-size:11px;color:var(--sub);text-align:right;flex:0 0 auto;line-height:1.5}
.card .m em{display:block;font-style:normal;color:var(--orange);font-weight:700;font-size:13px}

/* ---------- 設定 ---------- */
.set{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:4px 14px}
.row{display:flex;align-items:center;justify-content:space-between;padding:12px 0;border-bottom:1px solid var(--line)}
.row:last-child{border-bottom:none}
.row .lb{font-size:13.5px}
.row .lb small{display:block;color:var(--sub);font-size:10.5px;margin-top:2px}
.step{display:flex;align-items:center;gap:6px}
.step button{
  width:32px;height:32px;border-radius:9px;border:1px solid var(--line);background:#232a38;color:var(--txt);
  font-size:17px;cursor:pointer;line-height:1
}
.step button:active{background:#2e3646}
.step b{min-width:56px;text-align:center;font-size:15px;font-variant-numeric:tabular-nums}
.sw{width:50px;height:29px;border-radius:15px;background:#38414f;border:none;position:relative;cursor:pointer;transition:background .2s;flex:0 0 auto}
.sw::after{content:"";position:absolute;top:3px;left:3px;width:23px;height:23px;border-radius:50%;background:#fff;transition:left .2s}
.sw.on{background:var(--green)}
.sw.on::after{left:24px}

.note{
  margin:18px 0 0;background:rgba(255,210,61,.07);border:1px solid rgba(255,210,61,.28);
  border-radius:14px;padding:13px 14px;font-size:11.5px;line-height:1.7;color:#e7dfc4
}
.note b{color:var(--yellow);display:block;margin-bottom:5px;font-size:12px}

/* ---------- 実行画面 ---------- */
#run{position:fixed;inset:0;background:var(--bg);display:none;flex-direction:column;z-index:50}
#run.on{display:flex}
#run .top{padding:calc(14px + env(safe-area-inset-top,0px)) 18px 6px;display:flex;align-items:center;justify-content:space-between}
#run .top .x{background:none;border:none;color:var(--sub);font-size:15px;cursor:pointer;padding:6px}
#run .top .pg{font-size:12px;color:var(--sub);font-variant-numeric:tabular-nums;letter-spacing:.05em}
.bar{height:4px;background:#232a38;border-radius:3px;margin:0 18px;overflow:hidden}
.bar i{display:block;height:100%;width:0;background:linear-gradient(90deg,var(--orange),var(--red));transition:width .25s}
